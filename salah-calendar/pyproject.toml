[project]
name = "salah-calendar"
version = "1.0.0"
description = "Full-year Islamic daily routine calendar generator for Malmö, Sweden"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "requests>=2.28.0",
]

[project.scripts]
salah-calendar = "main:main"
